


<% if user_signed_in? %>

  <a href="/swipe" class="text-pink-500">Swipe</a>
  <a href="/swipes" class="text-pink-500">Swipes</a>
  <a href="/matches" class="text-pink-500">matches</a>
  <a href="/photos" class="text-pink-500">photos</a>

    <h1> MUJ PROMFIL </h1>

     <div class="p-3 flex flex-col rounded-xl w-64 items-center">
    <div class="rounded-xl shadow-lg">
       <% if current_user.profile.images.attached? %>
            <%= image_tag current_user.profile.images.first, class: "rounded-xl w-auto user-image " %>
      <% else %>
   <img src="https://www.vermontteddybear.com/dw/image/v2/BDKM_PRD/on/demandware.static/-/Sites-master-catalog-vtb/default/dw0c5c4cde/images/VTB/vtb-23846-18ohsosoftteddybear-lightbrown_feature4_20201012_0843.jpg?sw=600" class="rounded-xl w-full user-image "       />
            <% end %>


   
      
      <div class="flex justify-between">
        <div class="tname text-xl float-left p-4"><%= current_user.profile.name %> <span class="font-light"><%= calculate_age(current_user.profile.dob) %> lat
</span></div>
      
      </div>
      
      <div class="pl-4 pb-2 text-gray-400">
        <p>Bio: <%= current_user.profile.bio %></p>
        <p>Plec: <%= current_user.profile.gender %></p>
        <p>Skad: <%= current_user.profile.location %></p>
        <p>Zainteresowania: <%= current_user.profile.interests %></p>
        <p>Denominacja: <%= current_user.profile.denomination %></p>
        <p>Moj Kosciol: <%= current_user.profile.attended_church %></p>
        <p>Praca: <%= current_user.profile.work %></p>
        <p>Hobby: <%= current_user.profile.hobbies %></p>
        <p>Swiadectwo: <%= current_user.profile.testimony %></p>
      </div>
      
    </div>
<% else %>
    Zaloguj sie mordo
<% end %>